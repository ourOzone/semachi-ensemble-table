import Drawer from "components/common/Drawer";

const TeamAddDrawer = () => {
    return (
        <Drawer drawerId={'teamAdd'}>
            test
        </Drawer>
    );
};

export default TeamAddDrawer;